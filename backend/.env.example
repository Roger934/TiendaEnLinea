# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=tienda_componentes
DB_PORT=3306

# JWT
JWT_SECRET=tu_clave_super_secreta_cambiala_123456

# Server
PORT=3000

# Email (para nodemailer)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=tu_email@gmail.com
EMAIL_PASS=tu_password_de_app

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:5500
```

**Crear:** `/backend/.env` (con tus valores reales)

**Crear:** `/backend/.gitignore`
```
node_modules/
.env
*.log